/**
 * Production environment — replace apiBaseUrl with live API endpoints when ready.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/assets/mock',
  appName: 'Wallet Dashboard',
  defaultCurrency: 'USD',
  refreshIntervalMs: 60_000,
};
